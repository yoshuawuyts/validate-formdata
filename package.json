{
  "name": "validate-formdata",
  "description": "Data structure for validating form data",
  "repository": "yoshuawuyts/validate-formdata",
  "version": "2.0.2",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "start": "bankai start test",
    "test": "standard && npm run deps && browserify test | tape-run"
  },
  "dependencies": {},
  "devDependencies": {
    "bankai": "^8.1.1",
    "bel": "^5.0.2",
    "browserify": "^14.4.0",
    "choo": "^5.6.2",
    "dependency-check": "^2.9.1",
    "spok": "^0.8.1",
    "standard": "^10.0.2",
    "tape": "^4.6.3",
    "tape-run": "^3.0.0"
  }
}
